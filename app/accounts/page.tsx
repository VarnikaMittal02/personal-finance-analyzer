import AccountsPageComponent from "@/components/accounts-page";

export default function AccountsPage() {
  return import AccountsPageComponent from "@/components/accounts-page";

/**
 * Accounts Page component.
 * 
 * This component renders the AccountsPageComponent.
 * 
 * @returns {JSX.Element} The AccountsPageComponent.
 */
export default function AccountsPage() {
  return (
    <div>
      <AccountsPageComponent />
    </div>
  );
}<AccountsPageComponent />;
}